# Adaptive Model Router

This package separates semantic classification from deterministic model selection.

Files:
- SKILL.md
- router.py
- model_matrix.json
- routing_policy.json
- semantic_task_schema.json
- example_task.json

Important: `model_matrix.json` is seeded with the ARC values and Luna latency values discussed during design, but pricing is intentionally marked NEEDS_REFRESH. Refresh current official pricing before production use.
